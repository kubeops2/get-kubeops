apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "chase_chart.fullname" . }}-test-connection"
  labels:
    {{- include "chase_chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "chase_chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
